# Enigme 74: Le prix d'un café
Voilà l'occasion de proposer un code minimaliste !

Deux lignes de python suffisent à résoudre l'énigme pour peu qu'on accepte de reposer sur `itertools.permutations`.

Pour la lisibilité, on préfère, comme d'habitude, quelques lignes de plus et l'élégance d'un `NamedTuple`.